# Platform Behavior Specification

This folder contains `spec.md` and `params.json`, which together define the intended behavior of the WICC 2026 platform. The `spec.md` file contains a formal specification of gameplay rules and scoring mechanisms, while `params.json` contains configuration parameters for the platform.

This file is best viewed on the online viewer gracefully made by Claude: 